#include <stdio.h>

int main(){
    int length,width; 
    printf("The length is:");
    scanf("%d",&length);
    printf("The width is:");
    scanf("%d",&width);
    printf("The area of rectangle is:%d",length*width);
    return 0;
}